---
import H2 from "./H2.astro";
import H3 from "./H3.astro";

const { tone = "light" } = Astro.props;
const isLight = tone === "light";
const listItemClass = `list-disc mt-2 xl:text-xl ${isLight ? "text-[#f2f5ff]" : "text-[rgba(11,29,63,0.8)]"}`;
const bodyTextClass = `xl:text-xl ${isLight ? "text-[#f2f5ff]" : "text-[rgba(11,29,63,0.8)]"}`;
---

<section class={isLight ? "mx-[10%] my-auto" : ""}>
  <H2><span class=`${isLight ? "text-[#f2f5ff]" : "text-[#0b1d3f]"}`>集会に参加する前に……</span></H2>
  <div class={`flex items-center justify-center gap-8 ${isLight ? "text-[#f2f5ff]" : "text-[#0b1d3f]"}`}>
    <div class={bodyTextClass}>
      <H3>義務事項（しなければならないこと）</H3>
      <ul class="grid pl-4">
        <li class={listItemClass}>シピルカくんのアバターを使用してください。</li>
        <ul class="grid pl-4">
          <li class={listItemClass}>最低限、シピルカくんの顔（Body）を使用してください。</li>
        </ul>
        <li class={listItemClass}>スタッフの指示があった場合は、それに従ってください。</li>
        <ul class="grid pl-4">
          <li class={listItemClass}>
            混雑状況によっては、新規インスタンスに移動をお願いする場合があります。
          </li>
        </ul>
      </ul>
    </div>
    <img
      src="/images/shipilka-is-watching.png"
      alt="シピルカくんが指を立てて注意を促しているイラスト"
      class="hidden md:block max-h-[512px] justify-self-center rounded-r-[3rem] [mask-image:linear-gradient(to_right,rgba(0,0,0,0)_0%,rgba(0,0,0,1)_25%)]"
    />
  </div>
  <div class={`flex items-center justify-center gap-8 ${isLight ? "text-[#f2f5ff]" : "text-[#0b1d3f]"}`}>
    <img
      src="/images/shipilka-is-seeing.png"
      alt="シピルカくんが指を立てて注意を促しているイラスト"
      class="hidden md:block max-h-[512px] justify-self-center rounded-l-[3rem] [mask-image:linear-gradient(to_right,rgba(0,0,0,1)_75%,rgba(0,0,0,0)_100%)]"
    />
    <div>
      <H3>できること</H3>
      <ul class="grid pl-4">
        <li class={listItemClass}>サンプルアバターで参加することができます。</li>
        <ul class="grid pl-4">
          <li class={listItemClass}>
            サンプルアバターをお持ちでない場合は、スタッフにお声がけください。
          </li>
        </ul>
        <li class={listItemClass}>インスタンス内では撮影することができます。</li>
        <ul class="grid pl-4">
          <li class={listItemClass}>
            撮影した画像などはSNSや集会の告知などで使用することがあります。
          </li>
        </ul>
      </ul>
    </div>
  </div>
  <div class={`flex items-center justify-center gap-8 ${isLight ? "text-[#f2f5ff]" : "text-[#0b1d3f]"}`}>
    <div>
      <H3>禁止事項（してはいけないこと）</H3>
      <ul class="grid pl-4">
        <li class={listItemClass}>以下に該当するアバターは使用してはいけません。</li>
        <ul class="grid pl-4">
          <li class={listItemClass}>センシティブな内容が含まれるアバター</li>
          <li class={listItemClass}>アバターの名誉を毀損する極端なキメラアバター</li>
          <li class={listItemClass}>利用規約に違反するアバター</li>
          <li class={listItemClass}>違法アップロードされたアバター</li>
        </ul>
        <li class={listItemClass}>ハラスメント行為をしてはいけません。</li>
      </ul>
      スタッフの裁量で予告なくインスタンスから退場させていただくことがあります。
    </div>
    <img
      src="/images/shipilka-turns-upside-down.png"
      alt="シピルカくんが指を立てて注意を促しているイラスト"
      class="hidden md:block max-h-[512px] justify-self-center rounded-r-[3rem] [mask-image:linear-gradient(to_right,rgba(0,0,0,0)_0%,rgba(0,0,0,1)_25%)]"
    />
  </div>

</section>
